meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: cd glen && npm ci --only=production
run:
  command: cd glen && npm run start
  containerPort: 3000
  persistenceMount: /data
