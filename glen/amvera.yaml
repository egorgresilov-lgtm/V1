---
meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  additionalCommands: npm install --production

run:
  command: npm run start
  containerPort: 3000

env:
  NODE_ENV: production
  PORT: 3000
